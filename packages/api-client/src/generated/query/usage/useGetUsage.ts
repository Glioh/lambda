/* Generated by Kubb. Do not edit. */

import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import type { GetUsageStatus200, GetUsageStatus401 } from "../../types/GetUsage";
import { queryOptions } from "@tanstack/react-query";
import { getUsage } from "../../client/usage/getUsage";

export const getUsageQueryKey = () => [{ url: "/api/usage" }] as const;

type GetUsageQueryKey = ReturnType<typeof getUsageQueryKey>;

export function getUsageQueryOptions(
	config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
	const queryKey = getUsageQueryKey();
	return queryOptions<
		GetUsageStatus200,
		ResponseErrorConfig<GetUsageStatus401>,
		GetUsageStatus200,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			const { data } = await getUsage({
				...config,
				signal: config.signal ?? signal,
				throwOnError: true,
			});
			return data;
		},
	});
}
