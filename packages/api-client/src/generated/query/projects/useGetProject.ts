/* Generated by Kubb. Do not edit. */

import type { RequestConfig, ResponseErrorConfig } from "../../.kubb/client";
import type { GetProjectOptions, GetProjectStatus200, GetProjectStatus401, GetProjectStatus404, } from "../../types/GetProject";
import { queryOptions } from "@tanstack/react-query";
import { getProject } from "../../client/projects/getProject";

export const getProjectQueryKey = ({ path }: Omit<GetProjectOptions, "headers">) =>
	[{ url: "/api/projects/:projectId", params: path }] as const;

type GetProjectQueryKey = ReturnType<typeof getProjectQueryKey>;

export function getProjectQueryOptions(
	{ path }: GetProjectOptions,
	config: Partial<Omit<RequestConfig, "path" | "query" | "body" | "headers" | "url">> = {},
) {
	const queryKey = getProjectQueryKey({ path });
	return queryOptions<
		GetProjectStatus200,
		ResponseErrorConfig<GetProjectStatus401 | GetProjectStatus404>,
		GetProjectStatus200,
		typeof queryKey
	>({
		queryKey,
		queryFn: async ({ signal }) => {
			const { data } = await getProject({
				...config,
				path,
				signal: config.signal ?? signal,
				throwOnError: true,
			});
			return data;
		},
	});
}
